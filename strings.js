"use strict";
(() => {
    var _a;
    const batman = 'batman';
    const linternaverde = 'Linterna Verde';
    const robin = `Heroes: volcan negro`;
    // const abc = 123;
    console.log(`I, am ${batman}`);
    console.log(batman.toUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No esta presente');
})();
